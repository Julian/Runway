<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="Fixture"
        android:icon="@android:drawable/ic_menu_compass"
        android:allowBackup="false">

        <!-- Launchable, so it can be placed on the home screen, and a web-search handler. -->
        <activity
            android:name=".FixtureActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.WEB_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- A widget provider, so the tests have a widget to host. -->
        <receiver
            android:name=".FixtureWidget"
            android:label="@string/fixture_widget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fixture_widget" />
        </receiver>

        <!-- A widget that insists on being set up first, and its setup screen. -->
        <receiver
            android:name=".FixtureSetupWidget"
            android:label="@string/fixture_setup_widget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fixture_setup_widget" />
        </receiver>
        <activity
            android:name=".FixtureWidgetSetupActivity"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
    </application>

</manifest>
