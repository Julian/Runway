package com.grayvines.runway.fixture

import android.appwidget.AppWidgetProvider

/** Does nothing; it asks to be kept out of widget pickers, which is all the tests look at. */
class FixtureHiddenWidget : AppWidgetProvider()
